import { range } from './utils';

const gridStyles = {
  width: '100px',
  height: '100px',
  border: 'solid grey',
  borderRadius: '5px',
};

function Grid({ numRows, numCols }) {
  return (
    <div style={{}} className="grid">
      {/* {todo} */}
    </div>
  );
}

export default Grid;
